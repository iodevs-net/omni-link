{
  "mcpServers": {
    "omni-link": {
      "command": "node",
      "args": ["${workspaceFolder}/build/index.js"],
      "env": {
        "OMNI_LINK_WORKSPACE": "${workspaceFolder}/.."
      }
    }
  }
}
